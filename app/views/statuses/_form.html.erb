<%= form_with(model: [@user,@status], local: true) do |f| %>
  <%= f.label :content, "What do you want to say?" %><br>
  <%= f.text_field :content %><br>
  
  <%= f.submit class:"greenButton" %>
<% end %>