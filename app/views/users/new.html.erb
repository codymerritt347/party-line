<% if @user.errors.any? %>
  <div class="errorWindow">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
    <% @user.errors.each do |error| %>
      <p><%= error.full_message %></p>
    <% end %>
  </div>
<% end %>

<div class="simpleWindow">
  <h1>🎉 Join the Party! 🎉</h1>
  <%= form_with(model: @user) do |f| %>
    <%= f.label :first_name, "👤 First Name:" %><br>
    <%= f.text_field :first_name %><br>

    <%= f.label :last_name, "👤 Last Name:" %><br>
    <%= f.text_field :last_name %><br>

    <%= f.label :email, "📧 Email:" %><br>
    <%= f.email_field :email %><br>

    <%= f.label :password, "🔑 Password:" %><br>
    <%= f.password_field :password %><br>

    <%= f.label :password_confirmation, "🔑 Confirm Password:" %><br>
    <%= f.password_field :password_confirmation %><br>
    <%= f.fields_for :statuses do |status_form| %>
      <%= status_form.label :content, "💬 First Status Update:" %><br>
      <p>Status messages are a way to tell your fellow party-goers<br>how you're doing or if you're available to chat.<p>
      <p><i>(Max of 281 characters, beat that Twitter!)</i></p>
      <%= status_form.text_area :content, maxlength: 281 %><br>
    <% end %>
    <div class="flex-container">
      <%= f.submit class: "blue button" %>
      <%= link_to "Login", login_path, class:"orange button" %>
    </div>
  <% end %>
</div>