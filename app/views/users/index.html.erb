<div class="simpleWindow">
  <h1>All Users</h1>
  <% @users.each do |user| %>
    <div class="userCard">
      <p>👤</p>
      <%= link_to user.screen_name, user_statuses_path(user) %>
    </div>
  <% end %>
</div>