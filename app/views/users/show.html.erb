<div class="simpleWindow">
  <h1><%= @user.screen_name %></h1>
  <% if !@user.statuses.empty? %>
    <div class="status-bubble">
      <%= @user.statuses.last.content %>
    </div>
    <%= @user.statuses.last.hours_ago %><br><br>
  <% end %>
  <%= render partial: "statuses/form", user:@user %>
</div>

<div class="simpleWindow">
  <h3><%= @user.screen_name %>'s Parties</h3>
  <% if @user.parties.count != 0 %>
    <% @user.parties.each do |party| %>
      <%= link_to party.name, party %><br>
    <% end %>
  <% else %>
    <h4>You have no parties. Let's start one!</h4>
  <% end %>
  <%= link_to "New Party", new_party_path, class:"blue button" %><br>
</div>

<div class="simpleWindow">
  <div class="flex-container">
    <%= link_to "Edit User", edit_user_path(@user), class:"orange button" %>
    <%= button_to "Delete User", @user, method: :delete, data: { confirm: "Are you sure?" }, class:"red button" %>
  </div>
</div>