<%= form_for @party do |f| %>
  <%= f.label :name, "Party Name" %><br>
  <%= f.text_field :name %><br>

  <%= f.collection_check_boxes(:user_ids, User.all, :id, :screen_name) %>
  <%= f.submit %>
<% end %>