<% if @reply.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this reply from being saved:</h2>
    <ul>
      <% @reply.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @reply) do |f| %>
  <%= f.label :content, "Reply Content" %><br>
  <%= f.text_area :content %><br>

  <%= f.label :user_id, "User ID" %><br>
  <%= f.collection_select :user_id, User.all, :id, :screen_name %><br>

  <%= f.label :message_id, "Message ID" %><br>
  <%= f.collection_select :message_id, Message.all, :id, :content %><br>

  <%= f.submit %>
<% end %>